interface:
  display_name: "Metagent"
  short_description: "Analyze agent tooling and MCP surfaces"
  default_prompt: "Use $metagent to analyze this agent workflow or MCP setup and separate configured, authenticated, loaded, and verified state."

policy:
  allow_implicit_invocation: true
